<Window x:Class="IDE.View.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local ="clr-namespace:IDE.View"
        xmlns:z ="clr-namespace:IDE"
        mc:Ignorable="d"
        MinHeight="250" MinWidth="350"
        Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="{DynamicResource file}">
                <MenuItem Header="{DynamicResource create}"/>
                <MenuItem Header="{DynamicResource open}"/>
                <MenuItem Header="{DynamicResource save}"/>
                <MenuItem Header="{DynamicResource save_as}"/>
                <MenuItem Header="{DynamicResource exit}"/>
            </MenuItem>
            <MenuItem Header="{DynamicResource edit}">
                <MenuItem Header="{DynamicResource undo}"/>
                <MenuItem Header="{DynamicResource redo}"/>
                <MenuItem Header="{DynamicResource copy}"/>
                <MenuItem Header="{DynamicResource paste}"/>
                <MenuItem Header="{DynamicResource select_all}"/>
            </MenuItem>
            <MenuItem Header="{DynamicResource text}">
                <MenuItem Header="{DynamicResource task}"/>
                <MenuItem Header="{DynamicResource grammar}"/>
                <MenuItem Header="{DynamicResource analyze_method}"/>
                <MenuItem Header="{DynamicResource error_diagnosis}"/>
                <MenuItem Header="{DynamicResource test_example}"/>
                <MenuItem Header="{DynamicResource reference_list}"/>
                <MenuItem Header="{DynamicResource source_code}"/>
            </MenuItem>
            <MenuItem Header="{DynamicResource run}"/>
            <MenuItem Header="{DynamicResource certificate}">
                <MenuItem Header="{DynamicResource show_certificate}"/>
                <MenuItem Header="{DynamicResource about}"/>
            </MenuItem>
        </Menu>
        
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Click="CreateClick">
                    <Image Source="../Resources/Assets/UI/create.png" Style="{StaticResource ResourceKey=toolbar_button_image}"/>
                    <Button.ToolTip>
                        <ToolTip Content="{DynamicResource create}"/>
                    </Button.ToolTip>
                </Button>
                <Button Click="OpenClick">
                    <Image Source="../Resources/Assets/UI/open.png" Style="{StaticResource toolbar_button_image}"/>
                    <Button.ToolTip>
                        <ToolTip Content="{DynamicResource open}"/>
                    </Button.ToolTip>
                </Button>
                <Button Click="SaveClick">
                    <Image Source="../Resources/Assets/UI/save.png" Style="{StaticResource toolbar_button_image}"/>
                    <Button.ToolTip>
                        <ToolTip Content="{DynamicResource save}"/>
                    </Button.ToolTip>
                </Button>

                <Separator/>
                <Button>
                    <Image Source="../Resources/Assets/UI/undo.png" Style="{StaticResource toolbar_button_image}"/>
                    <Button.ToolTip>
                        <ToolTip Content="{DynamicResource undo}"/>
                    </Button.ToolTip>
                </Button>
                <Button>
                    <Image Source="../Resources/Assets/UI/redo.png" Style="{StaticResource toolbar_button_image}"/>
                    <Button.ToolTip>
                        <ToolTip Content="{DynamicResource redo}"/>
                    </Button.ToolTip>
                </Button>

                <Separator/>
                <Button>
                    <Image Source="../Resources/Assets/UI/copy.png" Style="{StaticResource toolbar_button_image}"/>
                    <Button.ToolTip>
                        <ToolTip Content="{DynamicResource copy}"/>
                    </Button.ToolTip>
                </Button>
                <Button>
                    <Image Source="../Resources/Assets/UI/cut.png" Style="{StaticResource toolbar_button_image}"/>
                    <Button.ToolTip>
                        <ToolTip Content="{DynamicResource cut}"/>
                    </Button.ToolTip>
                </Button>
                <Button>
                    <Image Source="../Resources/Assets/UI/paste.png" Style="{StaticResource toolbar_button_image}"/>
                    <Button.ToolTip>
                        <ToolTip Content="{DynamicResource paste}"/>
                    </Button.ToolTip>
                </Button>

                <Separator/>
                <Button>
                    <Image Source="../Resources/Assets/UI/run.png" Style="{StaticResource toolbar_button_image}"/>
                    <Button.ToolTip>
                        <ToolTip Content="{DynamicResource run}"/>
                    </Button.ToolTip>
                </Button>

                <Separator/>
                <Button>
                    <Image Source="../Resources/Assets/UI/cert.png" Style="{StaticResource toolbar_button_image}"/>
                    <Button.ToolTip>
                        <ToolTip Content="{DynamicResource certificate}"/>
                    </Button.ToolTip>
                </Button>
                <Button>
                    <Image Source="../Resources/Assets/UI/info.png" Style="{StaticResource toolbar_button_image}"/>
                    <Button.ToolTip>
                        <ToolTip Content="{DynamicResource about}"/>
                    </Button.ToolTip>
                </Button>
                
            </ToolBar>
        </ToolBarTray>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <RichTextBox Name="CodeTextBox"
                Grid.Row="0" Margin="10">
            </RichTextBox>
            <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="3"/>
            <DataGrid Grid.Row="2" Margin="10">
                
            </DataGrid>
        </Grid>
    </DockPanel>
</Window>
